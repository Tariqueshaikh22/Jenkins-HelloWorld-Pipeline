pipeline {
    agent any
        stages { 
            stage('compile java code') {
                steps { 
                    echo "HelloWorld"
                
                
            }
            
            
        }
           }
}
